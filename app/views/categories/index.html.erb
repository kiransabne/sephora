<!DOCTYPE html>
<html>
<head>
  <title>categories</title>
  <style type="text/css">
  	th, td {
    padding: 10px;
}
ln{color:#169457;
padding -left: 5px, 5px;
margin-left:30px;
font-family: Courier New;
font-size:120%;
font-weight: 600;
line-height:30px
}
button
  {
  width: auto;
    background-color: #4CAF50;
    color: white;
    font-size: 120%;
    padding: 8px 12px;
    margin: 8px 0;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }
  button:hover{
    background-color: #45a049;
  }
  td, th {
    border: 1px solid #dddddd;
    text-align: left;
    padding: 8px;
}

tr:nth-child(even) {
    background-color: #A3D69B;
}
  </style>
</head>
<body>
  <h1>All Categories</h1>
  <table style="width:100%">
  	<th>category</th>
  	<th>TotalSub-category</th>
  	<th>Total Sub-sub-category</th>
  	<th>Total products</th>
  <% @categories.each do |category| %>
  <tr align = "center">
  <td><%= link_to category.category_name, category_path(id: category.id) %> </td>
  <td><%= category.sub_categories.count %> </td>
  <td><%= category.sub_sub_categories.count %> </td>
  <td><%= category.products.count %> </td>
</tr>
  <% end %>
</table>
</body>
</html>